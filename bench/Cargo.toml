[package]
name = "bench"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Benchmarking harness for Vivy"
publish = false

[dependencies]
vivy-core = { path = "../lib" }
rand = "0.9"
