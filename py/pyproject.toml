[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "vivy"
version = "0.1.0"
description = "Vector search that lives on one machine"
requires-python = ">=3.8"
license = "Apache-2.0"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "vivy"
